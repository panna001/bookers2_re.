<h1>New book</h1>
<%= form_with model: model, local: true do |f| %>
  <%= f.label :title, "Title" %>
  <%= f.text_field :title %>
  <%= f.label :body, "Opinion" %>
  <%= f.text_area :body %>
  <%= f.submit "Create Book" %>
<% end %>
